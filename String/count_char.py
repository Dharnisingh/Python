# The string count() method returns the number of occurrences of a substring in the given string.

text = "Count the number of spcified character in this string"

res = text.count('i')
print(res)
res = text.count("the")
print(res)
